---
const { title, color } = Astro.props;
---

<section>
  <div class="circle" style={`--circle-color: ${color};`}>
    <span>{title}</span>
  </div>
</section>

<style>
  span {
    font-family: "rubiksemibold";
  }
  @media (min-width: 1025px) {
    .circle {
      width: 64px;
      height: 64px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: var(--circle-color);
      border: 2px solid var(--price-button-default-border);
    }

    .circle[style*="--circle-color: var(--border-accent)"] {
      border: none;
      color: #ffffff;
    }
  }
  @media (max-width: 599px) {
    .circle {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: var(--circle-color);
      border: 2px solid var(--price-button-default-border);
    }

    .circle[style*="--circle-color: var(--border-accent)"] {
      border: none;
      color: #ffffff;
    }
  }
  @media (min-width: 600px) and (max-width: 1024px) {
    .circle {
      width: 64px;
      height: 64px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: var(--circle-color);
      border: 2px solid var(--price-button-default-border);
    }

    .circle[style*="--circle-color: var(--border-accent)"] {
      border: none;
      color: #ffffff;
    }
  }
</style>
