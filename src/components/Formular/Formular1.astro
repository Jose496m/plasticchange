---
import Stepbar from "./Stepbar.astro";
import Menu from "../header/Menu.astro";
import FormOverskrift from "./FormOverskrift.astro";
import Button from "../Button.astro";
import InputForm1 from "../InputForm1.astro";
import Prisbutton from "../Prisbutton.astro";
import Formularcard from "../Formular/Formularcard.astro";
import { Image } from "astro:assets";
import pic1 from "../../assets/images/støttebeviskop.png";
import pic2 from "../../assets/images/støttebevis.png";
---

<Menu variant="form" />
<section>
  <div class="oval-container">
    <div class="oval-section section1"></div>
    <div class="oval-section section2"></div>
    <div class="oval-section section3"></div>
    <div class="oval-section section4"></div>
  </div>
</section>
<FormOverskrift titel="Giv støtte som en julegave." />
<section>
  <div class="kort flex">
    <Formularcard
      class="card"
      text=" Unikt emaljekrus fra Plastic Change - inklusive et print-selv støttebevis. (Bemærk: Ramme medfølger ikke.) "
    >
      <Image class="støttebeviskop" src={pic1} alt="pic1" />
    </Formularcard>

    <Formularcard
      class="card"
      text=" Unikt Plastic Change emaljekrus - samt et print selv støttebevis.(Ramme medfølger ikke) 450kr. "
    >
      <Image class="støttebevis" src={pic2} alt="pic1" />
    </Formularcard>
  </div>
</section>
<div>
  <p class="ptag">
    Ønsker du at bidrage med en anden donation? <br />Gør det nemt herunder
  </p>
  <label class="labeltag">
    Vælg dit engangs beløb <span class="farve">*</span></label
  >
</div>

<div class="border spacing">
  <InputForm1 placeholder="Andet beløb" />
</div>
<div class="buttons">
  <Button variant="secondary" title="← Tilbage" link="/formular/step1" />
  <Button variant="arrow" title="Næste → " link="/formular/step2" />
</div>

<style>
  @media (min-width: 1025px) {
    section {
      margin-top: var(--spacing-44);
      margin-bottom: var(--spacing-32);
    }
    .oval-container {
      position: relative;
      width: 700px;
      height: 16px;
      overflow: hidden;
      border: var(--border-1) solid var(--border-dark);
    }

    .oval-section {
      position: absolute;
      height: 100%;
    }

    .section1 {
      left: 10%;
      width: 20%;
      border: 1px solid var(--border-dark);
      border-bottom-left-radius: var(--border-16);
      border-top-left-radius: var(--border-16);
      background-color: var(--color-red-300);
    }
    .section2 {
      left: 30%;
      width: 20%;
      border: 1px solid var(--border-dark);
    }
    .section3 {
      left: 50%;
      width: 20%;
      border: 1px solid var(--border-dark);
    }
    .section4 {
      left: 70%;
      width: 20%;
      border: 1px solid var(--border-dark);
      border-bottom-right-radius: var(--border-16);
      border-top-right-radius: var(--border-16);
    }

    .flex {
      display: flex;
      gap: 10%;
      flex-direction: row;
      justify-content: center;
    }
    .labeltag {
      margin-left: var(--spacing-64);
    }
    .ptag {
      margin-bottom: var(--spacing-44);
    }

    .buttons {
      display: flex;
      justify-content: space-between;
      margin-inline: var(--spacing-64);
      margin-block: var(--spacing-128);
      gap: var(--spacing-128);
    }

    .baggrund {
      background-color: var(--color-red-100);
      padding: 200px;
      margin-top: var(--spacing-252);
    }

    .inputs {
      margin-top: var(--spacing-32);
    }
    .farve {
      color: var(--color-red-300);
    }
    .flex-container {
      display: flex;
      gap: var(--spacing-24);

      margin-top: var(--spacing-32);
    }
    h2,
    p {
      text-align: center;
    }

    body {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .spacing {
      margin-top: var(--spacing-32);
    }
    .støttebeviskop {
      border-top-right-radius: var(--spacing-16);
      border-top-left-radius: var(--spacing-16);
      opacity: 0.5;
    }

    .støttebeviskop:hover {
      transform: scale(1);
      opacity: 1;
    }
    .støttebevis {
      border-top-right-radius: var(--spacing-16);
      border-top-left-radius: var(--spacing-16);
      opacity: 0.5;
    }

    .støttebevis:hover {
      transform: scale(1);
      opacity: 1;
    }
  }

  @media (min-width: 600px) and (max-width: 1024px) {
    section {
      margin-top: var(--spacing-32);
      margin-bottom: var(--spacing-32);
    }
    .oval-container {
      position: relative;
      width: 500px;
      height: 13px;
      border-radius: var(--border-16);
      overflow: hidden;
      border: var(--border-1) solid var(--border-dark);
    }

    .oval-section {
      position: absolute;
      height: 100%;
    }

    .section1 {
      left: 10%;
      width: 20%;
      border: 1px solid var(--border-dark);
      border-bottom-left-radius: var(--border-16);
      border-top-left-radius: var(--border-16);
    }
    .section2 {
      left: 30%;
      width: 20%;
      border: 1px solid var(--border-dark);
    }
    .section3 {
      left: 50%;
      width: 20%;
      border: 1px solid var(--border-dark);
    }
    .section4 {
      left: 70%;
      width: 20%;
      border: 1px solid var(--border-dark);
      background-color: var(--color-red-300);
      border-bottom-right-radius: var(--border-16);
      border-top-right-radius: var(--border-16);
    }

    .flex {
      display: flex;
      gap: 10%;
      flex-direction: row;
      justify-content: center;
    }
    .labeltag {
      margin-left: var(--spacing-64);
    }
    .ptag {
      margin-bottom: var(--spacing-44);
    }

    .buttons {
      display: flex;
      justify-content: space-between;
      margin-inline: var(--spacing-64);
      margin-block: var(--spacing-128);
      gap: var(--spacing-128);
    }

    .baggrund {
      background-color: var(--color-red-100);
      padding: 200px;
      margin-top: var(--spacing-252);
    }

    .inputs {
      margin-top: var(--spacing-32);
    }
    .farve {
      color: var(--color-red-300);
    }
    .flex-container {
      display: flex;
      gap: var(--spacing-24);

      margin-top: var(--spacing-32);
    }
    h2,
    p {
      text-align: center;
    }

    body {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .spacing {
      margin-top: var(--spacing-32);
    }
    .støttebeviskop {
      border-top-right-radius: var(--spacing-16);
      border-top-left-radius: var(--spacing-16);
    }

    .støttebevis {
      border-top-right-radius: var(--spacing-16);
      border-top-left-radius: var(--spacing-16);
    }
  }

  @media (max-width: 599px) {
    .flex {
      display: flex;
      gap: 5%;
      flex-direction: row;
      justify-content: center;
    }
    .buttons {
      display: flex;
      justify-content: space-between;
      margin-inline: var(--spacing-44);
      gap: var(--spacing-44);
    }
    .pic1 {
      border-radius: var(--spacing-16);
      width: 263px;
      height: 325px;
    }
    .pic2 {
      border-radius: var(--spacing-16);
      width: 263px;
      height: 325px;
    }
    .pic3 {
      border-radius: var(--spacing-16);
      width: 263px;
      height: 325px;
    }

    .baggrund {
      background-color: var(--color-red-100);
    }

    .ptag {
      color: var(--color-mud-200);
    }
    .inputs {
      margin-top: var(--spacing-32);
    }
    .farve {
      color: var(--color-red-300);
    }
    .flex-container {
      display: flex;
      gap: var(--spacing-24);

      margin-top: var(--spacing-32);
    }
    h2,
    p {
      text-align: center;
    }

    body {
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .buttons {
      justify-content: space-between;
      margin-inline: var(--spacing-64);
      margin-top: var(--spacing-64);
      margin-bottom: var(--spacing-64);
    }
    .støttebeviskop {
      border-top-right-radius: var(--spacing-16);
      border-top-left-radius: var(--spacing-16);
    }
    .støttebevis {
      border-top-right-radius: var(--spacing-16);
      border-top-left-radius: var(--spacing-16);
    }
    .labeltag {
      margin-left: var(--spacing-64);
    }
    .ptag {
      margin-bottom: var(--spacing-44);
    }

    section {
      margin-top: var(--spacing-44);
      margin-bottom: var(--spacing-32);
    }
    .oval-container {
      position: relative;
      width: 340px;
      height: 8px;
      overflow: hidden;
      border: var(--border-1) solid var(--border-dark);
    }

    .oval-section {
      position: absolute;
      height: 100%;
    }

    .section1 {
      left: 10%;
      width: 20%;
      border: 1px solid var(--border-dark);
      border-bottom-left-radius: var(--border-16);
      border-top-left-radius: var(--border-16);
      background-color: var(--color-red-300);
    }
    .section2 {
      left: 30%;
      width: 20%;
      border: 1px solid var(--border-dark);
    }
    .section3 {
      left: 50%;
      width: 20%;
      border: 1px solid var(--border-dark);
    }
    .section4 {
      left: 70%;
      width: 20%;
      border: 1px solid var(--border-dark);
      border-bottom-right-radius: var(--border-16);
      border-top-right-radius: var(--border-16);
    }
  }
</style>
